controller:
  podSecurityContext:
    fsGroup: {{ .uid }}
  securityContext:
    runAsUser: {{ .uid }}

webhook:
  podSecurityContext:
    fsGroup: {{ .uid }}
  securityContext:
    runAsUser: {{ .uid }}